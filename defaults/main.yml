---
user: '{{ ansible_user|default(ansible_user_id) }}'
home: '/home/{{ user }}'
shell: '/bin/bash'

ssh_pub_key: https://github.com/xkoralsky.keys

disable_password_auth: true
disable_root_login: false

run_raw: false

sudo_setup: yes
sudo_nopasswd: yes
